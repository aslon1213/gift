{
    "$schema": "https://schema.railpack.com",
    "steps": {
        "build": {
            "commands": [
                "mise run web.build"
            ]
        }
    },
    "deploy": {
        "startCommand": "mise run web.start"
    }
}