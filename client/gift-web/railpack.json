{
    "$schema": "https://schema.railpack.com",
    "steps": {
        "build": {
            "inputs": [
                {
                    "local": true,
                    "include": [
                        "."
                    ]
                }
            ],
            "commands": [
                "npm run build"
            ]
        }
    },
    "deploy": {
        "startCommand": "npx vite preview --host 0.0.0.0 --port ${PORT:-4173}"
    }
}